<%= simple_form_for [@project, @task] do |f| %>
  <%= f.input :name %>
  <%= f.input :description, as: :text %>
  <%= f.input :due_date, as: :date_picker %>
  <%= f.input :completion, as: :boolean %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>

