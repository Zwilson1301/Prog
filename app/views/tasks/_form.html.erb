<%= simple_form_for [@project, @task] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :due_date %>
  <%= f.input :completion %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>

